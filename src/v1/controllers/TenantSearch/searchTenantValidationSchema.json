{
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "key": {
      "type": "string",
      "enum": ["tenant", "tenant_board"],
      "description": "key is required"
    },
    "filters": {
      "type": "object",
      "properties": {
        "tenant_name": {
          "type": "string",
          "description": "The name of the tenant"
        },
        "tenant_type": {
          "type": "string",
          "description": "The name of the tenant"
        },
        "status": {
          "type": "string",
          "enum": ["approved", "draft", "deleted"],
          "description": "The status of the tenant"
        },
        "is_active": {
          "type": "boolean",
          "description": "must be a boolean and is required"
        },
        "updated_by": {
          "type": "number",
          "description": "The ID of the user updating the tenant board"
        },
        "name": {
          "type": "string",
          "description": "The name of the tenant board"
        },
        "class_id": {
          "type": "array",
          "items": {
            "type": "number"
          },
          "minItems": 1,
          "uniqueItems": true,
          "description": "Array of class IDs"
        },
        "created_by": {
          "type": "number",
          "description": "The ID of the user creating the tenant board"
        },
        "offset": {
          "type": "number",
          "description": "The offset to set number for exclude"
        },
        "limit": {
          "type": "number",
          "description": "The limit to set number record per page"
        }
      },
      "additionalProperties": false
    }
  }
}
